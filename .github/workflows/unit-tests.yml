name: Unit-Tests

on:
  workflow_call:

jobs:
  Unit-Tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: test
        run: |
          npm install
          npm test
