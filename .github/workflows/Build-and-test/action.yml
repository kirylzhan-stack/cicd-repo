name: Build and Test

runs:
  using: "composite"
  steps:
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: "20"
        cache: "npm"

    - name: Build and Test
      shell: bash
      run: |
        npm ci
        npm run lint
        npm test
        npm run build
